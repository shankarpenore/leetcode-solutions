class Solution {
    public int addDigits(int num) {
        
        if(num == 0) return 0;
        if(num%9 == 0) return 9;
        
        return num%9;
        //     int sum = 0 ;
        //     int rem = 0;
        //     while(num >= 10){
        //         rem = num%10;
        //         num = num / 10;
        //         num = num + rem;
        //     }
        // return num;
    }
}
